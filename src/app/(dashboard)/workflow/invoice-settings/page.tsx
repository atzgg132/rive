"use client";

import { useEffect, useState } from "react";
import Link from "next/link";
import { ArrowLeft } from "lucide-react";
import { toast } from "sonner";
import { Button, Input, Textarea } from "@/components/ui";

export default function InvoiceSettingsPage() {
  const [form, setForm] = useState({ businessName: "", contactName: "", email: "", phone: "", address: "", taxId: "", logoUrl: "", defaultCurrency: "USD", invoicePrefix: "INV", paymentInstructions: "", defaultTerms: "" }); const [loading, setLoading] = useState(true); const [saving, setSaving] = useState(false);
  useEffect(() => { void fetch("/api/workflow/invoice-profile", { cache: "no-store" }).then((response) => response.json()).then((data) => { if (data?.profile) setForm((current) => ({ ...current, ...Object.fromEntries(Object.entries(data.profile).filter(([key]) => key in current)) })); }).finally(() => setLoading(false)); }, []);
  const update = (key: keyof typeof form, value: string) => setForm((current) => ({ ...current, [key]: value }));
  const submit = async (event: React.FormEvent) => { event.preventDefault(); setSaving(true); try { const response = await fetch("/api/workflow/invoice-profile", { method: "PUT", headers: { "Content-Type": "application/json" }, body: JSON.stringify(form) }); const data = await response.json(); if (!response.ok || !data?.success) throw new Error(data?.message || "Settings could not be saved."); toast.success("Invoice settings saved."); } catch (error) { toast.error(error instanceof Error ? error.message : "Settings could not be saved."); } finally { setSaving(false); } };
  if (loading) return <div className="p-10 text-sm text-muted-foreground">Loading invoice settings…</div>;
  return <div className="mx-auto max-w-3xl space-y-6"><Link href="/workflow/invoices/new" className="inline-flex items-center gap-2 text-sm font-medium text-muted-foreground hover:text-foreground"><ArrowLeft className="h-4 w-4" /> Back to invoice editor</Link><div><h1 className="mt-3 text-3xl font-bold tracking-tight">Invoice settings</h1><p className="mt-2 text-sm text-muted-foreground">These details appear on future invoice snapshots. Existing sent invoices remain immutable.</p></div><form onSubmit={submit} className="space-y-5"><section className="rounded-2xl border border-border bg-card p-5 shadow-sm"><h2 className="font-semibold">Your business</h2><div className="mt-4 grid gap-4 sm:grid-cols-2"><label className="text-xs font-semibold text-muted-foreground">Business name<Input value={form.businessName} onChange={(e) => update("businessName", e.target.value)} className="mt-2" /></label><label className="text-xs font-semibold text-muted-foreground">Contact name<Input value={form.contactName} onChange={(e) => update("contactName", e.target.value)} className="mt-2" /></label><label className="text-xs font-semibold text-muted-foreground">Invoice email<Input type="email" value={form.email} onChange={(e) => update("email", e.target.value)} className="mt-2" /></label><label className="text-xs font-semibold text-muted-foreground">Phone<Input value={form.phone} onChange={(e) => update("phone", e.target.value)} className="mt-2" /></label><label className="text-xs font-semibold text-muted-foreground sm:col-span-2">Business address<Textarea rows={3} value={form.address} onChange={(e) => update("address", e.target.value)} className="mt-2 resize-none" /></label><label className="text-xs font-semibold text-muted-foreground">Tax ID / GSTIN<Input value={form.taxId} onChange={(e) => update("taxId", e.target.value)} className="mt-2" /></label><label className="text-xs font-semibold text-muted-foreground">Logo URL<Input value={form.logoUrl} onChange={(e) => update("logoUrl", e.target.value)} className="mt-2" /></label></div></section><section className="rounded-2xl border border-border bg-card p-5 shadow-sm"><h2 className="font-semibold">Defaults</h2><div className="mt-4 grid gap-4 sm:grid-cols-2"><label className="text-xs font-semibold text-muted-foreground">Default currency<Input value={form.defaultCurrency} onChange={(e) => update("defaultCurrency", e.target.value.toUpperCase())} maxLength={3} className="mt-2 uppercase" /></label><label className="text-xs font-semibold text-muted-foreground">Invoice prefix<Input value={form.invoicePrefix} onChange={(e) => update("invoicePrefix", e.target.value)} maxLength={16} className="mt-2" /></label><label className="text-xs font-semibold text-muted-foreground sm:col-span-2">Payment instructions<Textarea rows={3} value={form.paymentInstructions} onChange={(e) => update("paymentInstructions", e.target.value)} className="mt-2 resize-none" placeholder="Bank details or payment instructions" /></label><label className="text-xs font-semibold text-muted-foreground sm:col-span-2">Default terms<Textarea rows={3} value={form.defaultTerms} onChange={(e) => update("defaultTerms", e.target.value)} className="mt-2 resize-none" placeholder="Payment due within…" /></label></div></section><div className="flex justify-end"><Button type="submit" disabled={saving}>{saving ? "Saving…" : "Save invoice settings"}</Button></div></form></div>;
}
